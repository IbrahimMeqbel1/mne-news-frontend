# تقرير تدقيق الكود — مشروع البوابة الإعلامية (mne-news-frontend)

تم إجراء فحص شامل وشامل لكود المصدر الخاص بمشروع البوابة الإعلامية لوزارة الاقتصاد الوطني الفلسطيني، وذلك بهدف تحديد الثغرات البرمجية، تحسين الأداء، وتعزيز جودة الكود البرمجي. يغطي هذا التقرير الجوانب الوظيفية، الأداء، الأمان، وقابلية الصيانة.

## ملخص تنفيذي

يوضح الجدول أدناه ملخصاً للمشاكل المكتشفة وتصنيفها حسب درجة الخطورة:

| فئة التدقيق | حرج | عالي | متوسط | منخفض | الإجمالي |
| :--- | :---: | :---: | :---: | :---: | :---: |
| الأخطاء الوظيفية (Bugs) | 0 | 1 | 1 | 0 | **2** |
| الأداء والتأخير (Performance) | 0 | 1 | 1 | 0 | **2** |
| الأمان (Security) | 0 | 1 | 0 | 0 | **1** |
| جودة الكود (Quality) | 0 | 0 | 2 | 1 | **3** |
| **الإجمالي العام** | **0** | **3** | **4** | **1** | **8** |

> **ملاحظة هامة:** لم يتم العثور على مشاكل "حرجة" تمنع تشغيل النظام، ولكن هناك 3 مخاطر "عالية" تتعلق بالأداء واستقرار الأنواع البرمجية يجب معالجتها فوراً لضمان تجربة مستخدم سلسة.

---

## 1. الأخطاء الوظيفية واستقرار الأنواع (Bugs & Typing)

تعتمد قوة مشاريع TypeScript على دقة تعريف الأنواع، وقد لوحظ وجود تساهل في استخدام النوع العام `any` في عدة مواضع حيوية.

| المشكلة المكتشفة | الموقع | درجة الخطورة | الحل المقترح |
| :--- | :--- | :---: | :--- |
| **إسقاط الأنواع (Type Erasure)** | `src/app/page.tsx:94` | **عالي** | استبدال النوع `any` بالواجهة المعرفة مسبقاً `NewsItem` لضمان التحقق من البيانات. |
| **عدم استقرار معالجة التواريخ** | `src/app/page.tsx:120` | **متوسط** | استخدام الدالة الموحدة `formatArabicDate` لضمان معالجة القيم الفارغة وتوحيد التنسيق. |

يعتبر استخدام `any` في حلقات التكرار (Loops) ثغرة برمجية صامتة، حيث قد يتغير هيكل البيانات القادم من الـ API دون أن ينبه النظام المطور لذلك، مما يؤدي إلى انهيار الواجهة الأمامية عند المستخدم النهائي.

---

## 2. تحليل الأداء وسرعة الاستجابة (Performance)

يعد أداء الواجهة الأمامية عاملاً حاسماً في ترتيب محركات البحث (SEO) وتجربة المستخدم، وقد تم رصد بعض الممارسات التي قد تبطئ تحميل الصفحة.

| المشكلة المكتشفة | الموقع | درجة الخطورة | التأثير المتوقع |
| :--- | :--- | :---: | :--- |
| **استخدام صور غير محسنة** | كافة ملفات العرض | **عالي** | زيادة حجم الصفحة ووقت التحميل (LCP) بسبب عدم استخدام `next/image`. |
| **منطق ترقيم الصفحات الثابت** | `src/app/news/page.tsx:173` | **متوسط** | صعوبة التنقل في حال زاد عدد الصفحات عن 5، حيث لا يتفاعل الترقيم مع الصفحة الحالية. |

يُنصح بشدة بتفعيل إعدادات الصور في `next.config.ts` والبدء باستخدام مكون `Image` من Next.js، حيث يوفر ميزات التحميل الكسول (Lazy Loading) وتغيير حجم الصور تلقائياً بناءً على جهاز المستخدم.

---

## 3. تقييم الأمان وحماية البيانات (Security)

بالرغم من أن المشروع يعتمد على عرض بيانات عامة، إلا أن طريقة عرض المحتوى النصي قد تشكل خطراً في حال تم اختراق قاعدة البيانات الخلفية.

> **تنبيه أمني:** يتم استخدام الخاصية `dangerouslySetInnerHTML` في ملف `src/app/news/[slug]/page.tsx` في السطر 57 لعرض محتوى الخبر. هذا الإجراء ضروري لعرض تنسيقات HTML، ولكنه يتطلب تنقية (Sanitization) للمحتوى لضمان عدم تنفيذ سكربتات ضارة (XSS) قد تكون مدرجة في النص.

---

## 4. جودة الكود وقابلية الصيانة (Code Quality)

تؤثر جودة الكود بشكل مباشر على سرعة تطوير الميزات الجديدة في المستقبل وسهولة فهم الكود من قبل مطورين آخرين.

| الملاحظة | الموقع | درجة الخطورة | التوصية |
| :--- | :--- | :---: | :--- |
| **تكرار العناصر الهيكلية** | `page.tsx` & `news/page.tsx` | **متوسط** | نقل الـ Header والـ Footer إلى مكونات مستقلة واستدعاؤها في `layout.tsx`. |
| **روابط وهمية (Dead Links)** | الفوتر وأيقونات التواصل | **منخفض** | استبدال الروابط التي تشير إلى `#` بروابط فعلية أو إخفاؤها لحين توفرها. |

---

## التوصيات والخطوات التالية

بناءً على نتائج التدقيق، نقترح ترتيب أولويات العمل كما يلي:

1.  **المرحلة الأولى (فورية):** تحديث كافة وسوم الصور إلى مكون `next/image` وتصحيح الأنواع البرمجية (TypeScript Types) في كافة الصفحات.
2.  **المرحلة الثانية (تحسين الهيكلية):** إعادة هيكلة المشروع لنقل العناصر المتكررة إلى `layout.tsx` وتفعيل مكتبة لتنقية الـ HTML.
3.  **المرحلة الثالثة (تجربة المستخدم):** تحسين منطق ترقيم الصفحات (Pagination) ليكون ديناميكياً بالكامل وتفعيل البحث والفلاتر في صفحة الأخبار.

---
**تم إعداد هذا التقرير بواسطة Manus AI لضمان أعلى معايير الجودة البرمجية.**
